import type {
    SPINNER_SIZES,
  } from "./spinner.constants";
  
  /* ======================================
     SPINNER TYPES
  ====================================== */
  
  export type SpinnerSize =
    (typeof SPINNER_SIZES)[number];